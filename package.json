{
  "name": "@djyde/tree-util",
  "version": "0.1.3",
  "main": "lib/index.js",
  "scripts": {
    "start": "tsc --watch",
    "test": "jest",
    "build": "npm run clean && tsc",
    "clean": "rm -rf lib",
    "prepublish": "npm run build"
  },
  "files": [
    "lib"
  ],
  "devDependencies": {
    "@types/node": "^12.12.30",
    "jest": "^25.1.0",
    "power-assert": "^1.6.1",
    "typescript": "^3.8.3"
  }
}
